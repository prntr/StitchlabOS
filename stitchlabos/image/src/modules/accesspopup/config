# AccessPopup Module Configuration
# Provides automatic WiFi AP fallback when no network is available

# AccessPopup settings
export ACCESSPOPUP_SSID="Stitchlab"
export ACCESSPOPUP_TIMER_INTERVAL="2min"
